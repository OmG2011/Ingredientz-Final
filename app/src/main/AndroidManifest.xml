<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ingredientz">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ingredientz">
        <activity android:name=".Store_Selector_Final"></activity>
        <activity android:name=".Priority_Page"
            android:parentActivityName=".cart"></activity>
        <activity android:name=".final_order" />
        <activity android:name=".Store_Selector" />
        <activity android:name=".MainActivity" />
        <activity android:name=".cart" />
        <activity
            android:name=".item_details"
            android:noHistory="true"
            android:parentActivityName=".HomePage" />
        <activity
            android:name=".signup_page"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Developer" />
        <activity
            android:name=".HomePage"
            android:launchMode="singleInstance" />
        <activity android:name=".splashPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="ocr" />
    </application>

</manifest>